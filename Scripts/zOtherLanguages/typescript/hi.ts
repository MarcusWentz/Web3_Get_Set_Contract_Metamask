// Run with 
// ts-node hi.ts

// Compile to javascript with 
// tsc hi.ts

const textToPrint = "Print this text."

printText(textToPrint)

function printText(person: string) {
    console.log(person)
}
      